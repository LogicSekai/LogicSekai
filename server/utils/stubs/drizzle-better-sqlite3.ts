/**
 * Production stub for drizzle-orm/better-sqlite3 (Cloudflare Workers build)
 * This module is never called in production — all SQLite code paths
 * are guarded by `process.env.NODE_ENV !== 'production'`.
 */
export const drizzle = (_db: any, _config?: any): any => null
export const sql = (..._args: any[]): any => null
